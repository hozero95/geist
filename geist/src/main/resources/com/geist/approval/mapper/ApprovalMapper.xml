<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 
	결재 페이지
	담당 : 김현선
 -->

<mapper namespace="com.geist.approval.mapper.ApprovalMapper">
	
	<!-- 결재 문서 생성 insert -->
	<insert id="appCreate">
		INSERT INTO approval 
		VALUES (#{app_no}, #{app_class}, #{app_title}, sysdate, 1)
	</insert>	
	<insert id="appReqCreate" >
		INSERT INTO 
			app_request (app_no, emp_no) (
			SELECT 
				a.app_no, e.emp_no 
			FROM 
				approval a, employee e 
			WHERE 
				a.app_no = #{app_no}
				and e.emp_no = #{emp_no}
			)
	</insert>	
	<insert id="appAgrCreate">
		INSERT INTO
		    app_agree (app_no, emp_no, agr_status) (
		    SELECT 
		        a.app_no, e.emp_no, a.app_status 
		    FROM 
		        approval a, employee e 
		    WHERE 
		        a.app_no = #{agrVo.app_no}
		        and e.emp_no = (
		            SELECT emp_no FROM employee 
		            WHERE dept_no = (
		                SELECT dept_no FROM employee WHERE emp_no = #{emp_no}
		            ) 
		            AND emp_position LIKE '%'||#{agrVo.emp_position}||'%'
		        )
		    )
	</insert>
	
	
	<!-- 결재 요청 조회 -->	
	<select id="reqListWithPaging" resultType="com.geist.approval.domain.ApprovalVO">
		<![CDATA[
			SELECT
				app_no, app_class, app_date, app_title, emp_name, app_status
			FROM
			(
				SELECT /*+ INDEX_DESC(app_request pk_app_request) */
					rownum rn, arq.app_no, a.app_class, to_char(app_date, 'YYYY-MM-DD') app_date, a.app_title, e.emp_name, a.app_status
				FROM
					approval a, employee e, app_request arq
				WHERE
					e.emp_no = arq.emp_no   
					and arq.app_no = a.app_no  
					and arq.emp_no = #{emp_no}
					and rownum <= #{cri.pageNum} * #{cri.amount}
			)
			WHERE
				rn > (#{cri.pageNum}-1) * #{cri.amount}
			ORDER BY app_no DESC
		]]>
	</select>	
	<!-- 결재 요청 상세 조회 -->
	<select id="reqDetail" resultType="com.geist.approval.domain.ApprovalReqDTO">
			SELECT 
				a.app_no, a.app_class, a.app_title, a.app_date, a.app_status
			FROM 
				approval a, employee e, app_request arq
			WHERE 
				e.emp_no = arq.emp_no 
				and arq.app_no = a.app_no
				and arq.emp_no = #{emp_no}
				and arq.app_no = #{app_no}
	</select>
	
	
	<!-- 결재 승인 조회 -->
	<select id="admitListWithPaging" resultType="com.geist.approval.domain.ApprovalVO">
		<![CDATA[
			SELECT
				app_no, app_class, app_date, app_title, emp_name, app_status
			FROM
			(
				SELECT /*+ INDEX_DESC(app_agree pk_app_agree) */
					rownum rn, arq.app_no, a.app_class, to_char(app_date, 'YYYY-MM-DD') app_date, a.app_title, e.emp_name, a.app_status
				FROM
					approval a, employee e, app_request arq, app_agree agr
				WHERE
					e.emp_no = arq.emp_no  
					and agr.app_no = arq.app_no   
					and agr.app_no = a.app_no  
					and agr.emp_no = #{emp_no}
					and rownum <= #{cri.pageNum} * #{cri.amount}
			)
			WHERE
				rn > (#{cri.pageNum}-1) * #{cri.amount}
			ORDER BY app_no DESC
		]]>
	</select>	
	<!-- 결재 승인 상세 조회 -->
	<select id="admitDetail" resultType="com.geist.approval.domain.ApprovalAgrDTO">
		<![CDATA[
			SELECT 
				dept_name, emp_position, emp_name, app_title, app_date
			FROM (
			    SELECT 
			        d.dept_name, e.emp_position, e.emp_name, a.app_title, to_char(app_date, 'YYYY-MM-DD') app_date
			    FROM 
			        approval a, employee e, app_request arq, app_agree agr, department d
			    WHERE 
			        e.emp_no = arq.emp_no 
			        and e.dept_no = d.dept_no
			        and agr.app_no = arq.app_no   
			        and agr.app_no = a.app_no  
			        and agr.app_no = #{app_no}
			        and agr.emp_no = #{emp_no} 
			)
		]]>
	</select>
	
	
	<!-- 결재 승인 or 반려 -->
	<update id="appAdmit">
		UPDATE 
			app_agree 
		SET 
			agr_status = #{agr_status} 
		WHERE 
			emp_no = #{emp_no} 
			and app_no = #{app_no}
	</update>
	
	<!-- 최종 승인 상태 update -->
	<update id="finalState">
		UPDATE approval SET app_status = 2 WHERE app_no = (
			SELECT DISTINCT app_no FROM app_agree 
			WHERE
			(SELECT count(*) FROM app_agree WHERE agr_status in (2) and app_no = #{app_no})
			= (select count(*) FROM app_agree WHERE app_no = #{app_no})
			and app_no = #{app_no}
		)
	</update>
	
	<select id="getCount" resultType="int">
		SELECT 
			count(*) 
		FROM 
			approval a, employee e, app_request arq
 		WHERE
 			e.emp_no = arq.emp_no 
 			and arq.app_no = a.app_no 
 			and arq.emp_no = #{emp_no}		
	</select>
</mapper>